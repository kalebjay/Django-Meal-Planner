Meal Planner Web App

This Django based app uses a calendar to show meals and creates a meal plan
by retrieving stored recipes. The app also has a basic inventory management 
system to keep track of quantities of items used. 